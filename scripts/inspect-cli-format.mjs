import {execFile} from 'node:child_process';import{promisify}from'node:util';
try {const r=await promisify(execFile)('node_modules/@supabase/cli-windows-x64/bin/supabase.exe',['migration','list','--linked'],{windowsHide:true,timeout:60000});const x=JSON.parse(r.stdout);function shape(v,depth=0){if(depth>3)return typeof v;if(Array.isArray(v))return v.slice(0,1).map(x=>shape(x,depth+1));if(v&&typeof v==='object')return Object.fromEntries(Object.entries(v).map(([k,x])=>[k,shape(x,depth+1)]));return typeof v;}console.log(JSON.stringify(shape(x)));}catch{console.log('NO_JSON_FORMAT');}
